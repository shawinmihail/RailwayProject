package db.responses;

import java.util.HashMap;

public class TrainsListResponseMessage extends ResponseMessage {

    public TrainsListResponseMessage(HashMap[] answerList) {
        super(answerList);
    }
}
