package db.responses;

import java.util.HashMap;

public class TimesListResponseMessage extends ResponseMessage {

    public TimesListResponseMessage(HashMap[] answerList) {
        super(answerList);
    }
}
