package db.responses;

import java.util.HashMap;

public class StationsResponseMessage extends ResponseMessage {

    public StationsResponseMessage(HashMap[] answerList) {
        super(answerList);
    }
}
