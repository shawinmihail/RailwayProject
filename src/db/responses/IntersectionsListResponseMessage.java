package db.responses;

import java.util.HashMap;

public class IntersectionsListResponseMessage extends ResponseMessage {

    public IntersectionsListResponseMessage(HashMap[] answerList) {
        super(answerList);
    }
}
